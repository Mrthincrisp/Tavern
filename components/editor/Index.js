export {
  RichTextEditor, InlineStyleControls, BlockStyleControls, StyleButton,
} from './Editor';

// export page for the components in Editor.js generally only done when there are a large amoount of components in one page. Used as a "middle man" to reduce import data cluter when impoting components.
